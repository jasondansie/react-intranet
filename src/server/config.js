const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "gctestman",
      password: "C4rPnxT82r",
      database: "gctest",
      dialect: "mysql",
    },
    listPerPage: 10,
  };
  module.exports = config;