const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "gctestman",
      password: "C4rPnxT82r",
      database: "gctest",
      dialect: "mysql",
    },
    listPerPage: 10,
  };
  module.exports = config;